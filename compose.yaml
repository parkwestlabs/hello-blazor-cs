services:
  web-app:
    image: my-blazor-app
    build: .
    ports:
      - "80:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      # optional: overwrite value in appsettings.Development.json
      - DataProtectionSettings__AppSecretKey=local-development-secret-12345
